# API keys for this example.
#
# This example uses Ollama (local models - no API key required).
# Replace OPENAI_BASE_URL with your Ollama endpoint:
#   Docker Desktop: http://host.docker.internal:11434/v1
#   Linux host:     http://172.17.0.1:11434/v1
#
# Apply:
#   kubectl apply -f secret.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: ark-api-keys
  namespace: ark-teams
stringData:
  AGENT_PROVIDER: openai
  OPENAI_API_KEY: ollama
  OPENAI_BASE_URL: http://host.docker.internal:11434/v1
